/**
 * SCALE_MODE_META_MAP — 调式定义表(46 个)
 *
 * monorepo 重构:数据已移至 shared/data/scale-modes.json
 * 此文件保留为转发层,消费方 import 路径不变。
 */
import { SCALE_MODE_META_MAP } from "../../data-loader"
export { SCALE_MODE_META_MAP }
